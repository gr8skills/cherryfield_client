import React from 'react';

import classes from './search-overlay.module.css';

const SearchOverlay = () => {

    return (
        <section className={classes['search-overlay']}>

        </section>
    );
};

export default SearchOverlay;
